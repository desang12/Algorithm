package introductionToAlgorithms.chap2.InsertionSort;
/*
 * 
 * 입력 : <a1, a2, ... , a3>
 * 출력 : a'1 <= a'2 <= ...<= a'n을 만족하는 입력 수열의 순열 <a'1, a'2, ... , a'n>
 * 
 */
public class InsertionSort {

	public static void main(String[] args) {
		
		/** Pseudo Code
		 INSERTION-SORT(A)
		 for j = 2 to A.length
		 	key = A[j]
		 	//A[j]를 정렬된 배열 A[1..j-1]에 삽입한다.
		 	i = j - 1
		 	while i > 0 그리고 A[i] > key
		 		A[i + 1] = A[i]
		 		i = i - 1;
		 	A[i + 1] = key
		 */
		
		
		
	}

}
